{
  "name": "speech-srt",
  "version": "1.0.0",
  "description": "Speech to SRT converter with translation",
  "scripts": {
    "start": "node server/server.js",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "npm install",
    "build": "npm run build:backend && npm run build:frontend",
    "prod": "concurrently \"npm run prod:frontend\" \"npm run prod:backend\"",
    "dev": "concurrently \"npm run start\" \"cd frontend && npm run dev\"",
    "prod:frontend": "vite --port 5173",
    "prod:backend": "nodemon server/server.js",
    "install:all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "aws-sdk": "^2.1531.0",
    "axios": "^1.6.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "uuid": "^9.0.1",
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.3"
  }
}
